<div class="outline row mx-5 mt-5">
  <div class="col-md-8 cart d-flex flex-column justify-content-center gap-3">
    <div class="title">
      <div class="row">
        <div class="col">
          <h4><b>Shopping Cart</b></h4>
        </div>
        <div class="col align-self-center text-right text-muted">{{numberProduct}} items</div>
      </div>
    </div>
    {{#each data}}
    <div class="row main align-items-center">
      <div class="col-2"><img class="img-fluid" src={{this.image}}></div>
      <div class="col">
        <div class="row">{{this.productName}}</div>
      </div>
      <div class="col">
        <a href="#">-</a><a href="#" class="border">{{this.quantity}}</a><a href="#">+</a>
      </div>
      <div class="col">&dollar; {{this.price}} </div>
    </div>
    {{/each}}
    
    <div><a class="btn btn-info" href="/user/{{slug}}">&leftarrow; Continue Shop</a></div>
  </div>
  <div class="col-md-4 d-flex flex-column justify-content-center gap-3 summary">
    <div>
      <h5><b>Summary </b></h5>
    </div>
    <hr>
    <div class="row">
      <div class="col">ITEMS: {{numberProduct}}</div>
      <div class="col text-right">&dollar; {{total_price}}</div>
    </div>
    {{!-- <form>
      <label class = "mb-2 "for="customer-input">SHIPPING</label>
      <select name="shipping" class="form-control" id="customer-input">
        <option selected>Select Shipping Methods</option>
        <option value="1">Standard-Delivery- &dollar;5.00</option>
        <option value="2">Fast-Delivery- &dollar;10.00</option>
      </select>
    </form> --}}
    <div class="row" "padding: 2vh 0;">
      <div class="col">TOTAL PRICE</div>
      <div class="col text-right">&dollar; {{total_price}}</div>
    </div>
    <button type="button" class="btn btn-primary">CheckOut</button>
  </div>
</div>